export const TEXT_FORMATS = Object.freeze(["svg", "dxf"]);
export const IMAGE_FORMATS = Object.freeze(["png", "jpg", "jpeg", "bmp", "gif"]);
export const DOCUMENT_FORMATS = Object.freeze(["modcut"]);

export const SUPPORTED_IMPORT_FORMATS = Object.freeze([...TEXT_FORMATS, ...IMAGE_FORMATS, ...DOCUMENT_FORMATS]);
